// Cloudflare Pages configuration file. Use this as deployment guidance only.
const config = {
  framework: "nextjs",
  site: "odishacyberops-in",
  build: {
    command: "npm run build",
    directory: "out",
  },
};

export default config;
